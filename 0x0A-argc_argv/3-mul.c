#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * atoi - convert string to integer
 * @argc: numbers of arguments
 * @argv: array of arguments
 * Return: interger converted from string
 */
int atoi(char *s)
{
	int i, d, n, len, f, digit;

	i = d = n = len = f digit = 0;

	while (s[len] != '\0')
		len++;
	while (i < len && f == 0)
	{
		if (s[i] == '*')
			++d;
		if (s[i] >= '0' && s[i] <= '9')
		{

		}
	}

}
