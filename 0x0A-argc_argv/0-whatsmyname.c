#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main - entry point
 * @argc: ouput number of peremeters entered
 * @argv: array of entered peremeters
 * Return: 0 (sucess)
 */
int main(int argc __attribute__((unused)), char *argv[])
{

	printf("%s\n", *argv);

	return (0);
}
